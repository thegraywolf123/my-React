import { render } from "@testing-library/react";

function Sola(){
    return(
        <div className="rick">
            <h1>know more about our home</h1>
            <p>The `not` keyword inverts the meaning of an entire media query. It  will only negate the specific media query it is applied to.
            (Thus, it will not apply to every media query in a comma-separated list of media queries.)
            The `not` keyword can't be used to negate an individual feature query, only an entire media query.
             The `not` is evaluated last in the following query:</p>
        </div>
    );
}
export default Sola;